import pandas as pd\nimport yfinance as yf\nfrom datetime import datetime, timedelta\n\n# Function to fetch historical OHLC data for a given ticker\ndef fetch_ohlc_data(ticker, start_date, end_date):\n    # Fetch data using yfinance\n    data = yf.download(ticker, start=start_date, end=end_date, interval='1h')\n    return data\n\nif __name__ == '__main__':\n    # Define the tickers and output file names\n    tickers = {'SPY': 'SPY_data.csv', 'MicroTaiex': 'MicroTaiex_data.csv'}\n    # Define the end date (current date) and the start date (1 year ago)\n    end_date = datetime.utcnow()\n    start_date = end_date - timedelta(days=365)\n\n    # Loop through each ticker and fetch the data\n    for ticker, file_name in tickers.items():\n        print(f'Fetching data for {ticker}')\n        ohlc_data = fetch_ohlc_data(ticker, start_date, end_date)\n        # Save the data to CSV\n        ohlc_data.to_csv(file_name)\n        print(f'Data for {ticker} saved to {file_name}')